const hip = require('./hip-requests.js');
const json = require('./sanityData.json');
/* hip.postJobOptions(hip.randomCategory(), '7467 - Queenstown', 3, '04000000', 'hiptest@mailinator.com', 'API Test', true, true, (res) => {
  hip.postJobWithQuestions(res, (jobid) => {
    console.log(jobid);
  });
});*/

/* for (i = 0; i < 4; i++)	{
  time = !!i;
  console.log(time);
}*/

/* hip.uploadAttachments(function(result)
{
 hip.postJobOptions(hip.randomCategory(), '7467 - Queenstown', 3, '04000000', 'hiptest@mailinator.com', 'API Test', true, true, result, (res) => {
  hip.postJobWithQuestions(res, (jobid) => {
    console.log(jobid);
  });
});
});*/

hip.tradieKiwi('joeyngtradie@mailinator.com', function () {
  hip.getInvitedJobs((json) => {
    json.job_assignments.map((job) => {
      hip.declineJob(job.job_invitation_id, job.job_assignment_id, () => {
        //do nothing
      });
    });
    //let jobsArr = (arr) => JSON.parse(body).filter()
    //console.log('DONE!');
  });
});

